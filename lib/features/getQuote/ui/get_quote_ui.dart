import 'package:flutter/material.dart';

class GetQuoteUI extends StatelessWidget {
  const GetQuoteUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
